import 'package:flutter/material.dart';

class DispatchManager extends StatefulWidget {
  const DispatchManager({super.key});

  @override
  State<DispatchManager> createState() => _DispatchManagerState();
}

class _DispatchManagerState extends State<DispatchManager> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}